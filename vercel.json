{
  "version": 2,
  "buildCommand": "echo 'Static HTML project - no build needed'",
  "outputDirectory": ".",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}
